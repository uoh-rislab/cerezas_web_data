services:
  climate-reporting:
    volumes:
      # Required only when config/email.yaml uses delivery_method: gmail_api.
      # SMTP + App Password can use GMAIL_APP_PASSWORD from .env instead.
      - "${GOOGLE_SERVICE_ACCOUNT_HOST_PATH}:/run/secrets/gmail-service-account.json:ro"
